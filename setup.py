import setuptools

setuptools.setup(
    name='roundabout_capacity',
    version='0.0.1',
    author='Angus Spence',
    packages=['modules_python', 'modules_vba'],
    install_requires=['numpy', 'xlwings']
)